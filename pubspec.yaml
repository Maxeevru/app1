name: emergency_alert
description: Симулятор РСЧС оповещений
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  telephony: ^0.2.0
  flutter_overlay_window: ^0.5.0
  audioplayers: ^4.1.0
  flutter_local_notifications: ^17.2.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
